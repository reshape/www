{
  "name": "reshape-www",
  "description": "reshape's web presence",
  "version": "0.0.0",
  "author": "Jeff Escalante",
  "ava": {
    "verbose": "true"
  },
  "bugs": "https://github.com/reshape/reshape-www/issues",
  "dependencies": {
    "babel-preset-latest": "^6.16.0",
    "image-webpack-loader": "^2.0.0",
    "reshape-standard": "^0.4.0",
    "spike-css-standards": "^0.1.2",
    "webpack": "^1.13.2"
  },
  "devDependencies": {
    "ava": "^0.16.0",
    "rimraf": "^2.5.4",
    "snazzy": "^5.0.0",
    "spike": "^0.12.1",
    "spike-core": "^0.12.1",
    "standard": "^8.4.0"
  },
  "homepage": "https://github.com/reshape/reshape-www",
  "main": "app.js",
  "private": "true",
  "repository": "https://github.com/reshape/reshape-www",
  "scripts": {
    "lint": "standard | snazzy",
    "precommit": "npm run lint -s",
    "pretest": "npm run lint -s",
    "test": "NODE_ENV=test ava"
  }
}
